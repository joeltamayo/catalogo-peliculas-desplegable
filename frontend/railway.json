{
    "build": {
        "env": {
            "NODE_ENV": "production"
        },
        "command": "npm install && npm run build -- --base-href=/"
    },
    "deploy": {
        "startCommand": "npx serve -s dist/frontend/browser",
        "restartPolicyType": "ALWAYS"
    }
}